# ci.yml
# python-installer-smoke
#
# Copyright © 2026 Dagitali LLC. All rights reserved.
#
# GitHub Actions workflow for continuous integration (CI).
#
# Responsibilities
# - Run cross-platform validation for the repository action.
# - Confirm the action can provision the requested Python runtime on supported
#   GitHub-hosted runner operating systems.
# - Keep heavier action compatibility checks separate from the faster PR gate.
#
# Maintainer Notes
# - Global permissions are intentionally omitted; each job declares the minimum
#   permissions it needs.
# - External GitHub Actions are pinned to full commit SHAs to reduce supply-
#   chain risk.
# - Keep required pull-request and merge-queue gates in `pr.yml`.
# - The dynamic run name is part of the workflow contract: it should identify
#   the short-lived source branch and the long-running target branch for PR
#   review clarity.
#
# References
# - GitHub Actions metadata syntax: https://docs.github.com/en/actions/sharing-automations/creating-actions/metadata-syntax-for-github-actions
# - GitHub Actions expressions reference: https://docs.github.com/en/actions/learn-github-actions/expressions
# - GitHub Actions security hardening guide: https://docs.github.com/en/actions/security-guides/security-hardening-for-github-actions
# - GitHub Actions workflow syntax reference: https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions
# - GitHub pull request documentation: https://docs.github.com/en/pull-requests
# - GitHub merge queue documentation: https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/configuring-merge-queues

---

name: CI

run-name: >-
  ${{
    github.event_name == 'pull_request' &&
    format('CI: {0} -> {1}', github.head_ref, github.base_ref) ||
    github.event_name == 'merge_group' &&
    format('CI: merge queue for {0}', github.ref_name) ||
    'CI: manual dispatch'
  }}

on:
  pull_request:
    branches:
      # Long-lived GitFlow branches
      - main
      - develop
  merge_group:
    branches:
      - main
      - develop
  workflow_dispatch:

permissions: {}

jobs:
  action-compatibility:
    name: Action on ${{ matrix.os }} / Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ['3.11', '3.12', '3.13', '3.14']
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # Pinned v6.0.3
      - name: Run local action
        uses: ./
        with:
          python-version: ${{ matrix.python-version }}
      - name: Verify requested Python is active
        shell: bash
        run: |
          python --version
          python - <<'PY'
          import platform
          import sys

          expected = "${{ matrix.python-version }}"
          actual = platform.python_version()
          print(f"expected={expected}")
          print(f"actual={actual}")
          if not actual.startswith(expected + "."):
              sys.exit(f"Expected Python {expected}.x, got {actual}")
          PY

  optional-inputs:
    name: Optional inputs (${{ matrix.scenario.name }}) on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        scenario:
          - name: skip pip upgrade
            pip_version: ''
            upgrade_pip: 'false'
            pip_install: ''
            expected_pip_version: ''
            expected_package: ''
            expected_package_version: ''
          - name: pin pip version
            pip_version: '25.0.1'
            upgrade_pip: 'true'
            pip_install: ''
            expected_pip_version: '25.0.1'
            expected_package: ''
            expected_package_version: ''
          - name: install dependency
            pip_version: ''
            upgrade_pip: 'true'
            pip_install: 'packaging==25.0'
            expected_pip_version: ''
            expected_package: 'packaging'
            expected_package_version: '25.0'
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # Pinned v6.0.3
      - name: Run local action with optional inputs
        uses: ./
        with:
          python-version: '3.13'
          pip-version: ${{ matrix.scenario.pip_version }}
          upgrade-pip: ${{ matrix.scenario.upgrade_pip }}
          pip-install: ${{ matrix.scenario.pip_install }}
      - name: Verify optional input behavior
        shell: bash
        env:
          EXPECTED_PIP_VERSION: ${{ matrix.scenario.expected_pip_version }}
          EXPECTED_PACKAGE: ${{ matrix.scenario.expected_package }}
          EXPECTED_PACKAGE_VERSION: ${{ matrix.scenario.expected_package_version }}
        run: |
          python - <<'PY'
          import importlib.metadata
          import os

          pip_version = importlib.metadata.version("pip")
          print(f"pip={pip_version}")

          expected_pip_version = os.environ["EXPECTED_PIP_VERSION"]
          if expected_pip_version and pip_version != expected_pip_version:
              raise SystemExit(
                  f"Expected pip {expected_pip_version}, got {pip_version}"
              )

          expected_package = os.environ["EXPECTED_PACKAGE"]
          expected_package_version = os.environ["EXPECTED_PACKAGE_VERSION"]
          if expected_package:
              version = importlib.metadata.version(expected_package)
              print(f"{expected_package}={version}")
              if version != expected_package_version:
                  raise SystemExit(
                      f"Expected {expected_package} {expected_package_version}, "
                      f"got {version}"
                  )
          PY
